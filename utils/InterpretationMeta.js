export default class InterpretationMeta {

  constructor( id, name, text, url, imageURL, conversationName ) {
    this.id = id;
    this.name = name;
    this.text = text;
    this.url = url;
    this.imageURL = imageURL;
    this.conversationName = conversationName;
  }
}